# 🌦️ Weather Alert System - Architecture & Workflow Diagrams

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     WEATHER ALERT SYSTEM                             │
│                     (WeatherAlertSystem.tsx)                         │
└─────────────────────────────────────────────────────────────────────┘

                           ┌──────────────────┐
                           │   User Actions   │
                           └────────┬─────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
              ┌─────▼─────┐  ┌─────▼──────┐  ┌────▼──────┐
              │ Search    │  │ Geolocation│  │  Toggle   │
              │  City     │  │   Auto     │  │  Crops    │
              └─────┬─────┘  └─────┬──────┘  └────┬──────┘
                    │               │              │
                    └───────────────┼──────────────┘
                                    │
                    ┌───────────────▼──────────────┐
                    │  OpenWeatherMap API         │
                    │  (fetchCurrentWeather)      │
                    │  (fetchForecast)            │
                    │  (fetchWeatherByCoords)     │
                    └───────────────┬──────────────┘
                                    │
                    ┌───────────────▼──────────────┐
                    │   Parse Weather Data         │
                    │   (OpenWeatherData)          │
                    └───────────────┬──────────────┘
                                    │
                    ┌───────────────▼──────────────────┐
                    │ Calculate Weather Alerts         │
                    │ (calculateWeatherAlerts)         │
                    │ Compare with Crop Thresholds    │
                    └───────────────┬──────────────────┘
                                    │
                    ┌───────────────▼──────────────────┐
                    │   Alert Decision Tree            │
                    │ ├─ Temperature Check            │
                    │ ├─ Rainfall Check               │
                    │ ├─ Humidity Check               │
                    │ └─ Determine Risk Level         │
                    └───────────────┬──────────────────┘
                                    │
                    ┌───────────────▼──────────────────┐
                    │  Update Component State          │
                    │ (setCurrentAlerts)              │
                    │ (setWeatherData)                │
                    │ (setForecastData)               │
                    └───────────────┬──────────────────┘
                                    │
                    ┌───────────────┼──────────────────────┐
                    │               │                      │
              ┌─────▼──────┐  ┌────▼──────┐  ┌───────────▼──┐
              │   Render   │  │   Send    │  │   Display   │
              │   UI Cards │  │Notifications │   Forecast  │
              └────────────┘  └───────────┘  └─────────────┘
```

## Data Flow Diagram

```
INPUT SOURCES
     │
     ├─ 📍 Geolocation API ──┐
     │                       │
     ├─ 🔍 City Search ──────┤
     │                       ▼
     │           OpenWeatherMap API
     │           (api.openweathermap.org)
     │                       │
     │        ┌──────────────┼──────────────┐
     │        │              │              │
     │    (current)      (forecast)     (by coords)
     │        │              │              │
     └────────┼──────────────┼──────────────┘
              │              │
              └──────┬───────┘
                     │
              ▼──────────────▼──
        ┌────────────────────────────┐
        │  Parse API Response        │
        │  OpenWeatherData Interface │
        └────────────┬───────────────┘
                     │
             ┌───────▼───────┐
             │ Crop Selection│
             │   (State)     │
             └───────┬───────┘
                     │
        ┌────────────▼────────────┐
        │  Alert Calculation      │
        │  (Rules Engine)         │
        │                         │
        │ For Each Crop:         │
        │  ├─ Compare Temp        │
        │  ├─ Check Rainfall      │
        │  ├─ Check Humidity      │
        │  └─ Determine Risk      │
        └────────────┬────────────┘
                     │
        ┌────────────▼────────────────┐
        │  Alert Array                │
        │  {                          │
        │    crop: string             │
        │    emoji: string            │
        │    riskLevel: enum          │
        │    alerts: string[]         │
        │  }[]                        │
        └────────────┬────────────────┘
                     │
         ┌───────────┼───────────┐
         │           │           │
         ▼           ▼           ▼
    ┌────────┐  ┌─────────┐  ┌──────────┐
    │ Display│  │  Toast  │  │Push Alert│
    │ Cards  │  │Notif.   │  │(Optional)│
    │(🟢🟡🔴)│  │(Feedback)│  │          │
    └────────┘  └─────────┘  └──────────┘
```

## Component State Management

```
┌──────────────────────────────────────────────────────┐
│        WeatherAlertSystem Component State            │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─ Location State ─────────────────────────────┐  │
│  │ - city: string ('Gujarat')                   │  │
│  │ - lastUpdated: Date | null                   │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ Data State ─────────────────────────────────┐  │
│  │ - weatherData: OpenWeatherData | null        │  │
│  │ - forecastData: ForecastData | null          │  │
│  │ - currentAlerts: WeatherAlert[]              │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ UI State ───────────────────────────────────┐  │
│  │ - loading: boolean                           │  │
│  │ - error: string | null                       │  │
│  │ - notificationsEnabled: boolean              │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ User Preferences ───────────────────────────┐  │
│  │ - selectedCrops: string[] (['Rice', 'Wheat'])│  │
│  │                                              │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
└──────────────────────────────────────────────────────┘

State Transformations:
────────────────────

User enters city ──► setCity() ──┐
                                 ├─► handleFetchWeather() ──► setWeatherData()
User clicks location ────────────┘                      ├─► setForecastData()
                                                        ├─► setCurrentAlerts()
                                                        └─► setLastUpdated()

User toggles crop ──► setSelectedCrops() ──► calculateWeatherAlerts() ──► setCurrentAlerts()

User enables notifications ──► setNotificationsEnabled() ──► requestNotificationPermission()
```

## Crop Alert Decision Tree

```
                        Weather Data
                              │
                    ┌─────────┼─────────┐
                    │         │         │
                  Temp    Rainfall   Humidity
                    │         │         │
         ┌──────────┴─────────┴─────────┴──────────┐
         │                                          │
         ▼                                          ▼
    FOR EACH CROP:                           COMPARE WITH:
    ┌─────────────────────────┐             ┌──────────────────┐
    │ 🍚 Rice                 │             │ maxTemp          │
    │ 🌾 Wheat                │             │ minTemp          │
    │ 🤍 Cotton               │             │ minRainfall      │
    │ 🌽 Maize                │             │ maxRainfall      │
    │ 🥒 Sugarcane            │             │ optimalHumidity  │
    │ 🍌 Banana               │             │ (min/max)        │
    └────────────┬────────────┘             └──────────────────┘
                 │
                 │ Check each condition
                 │
        ┌────────┴────────┬─────────┐
        │                 │         │
        ▼                 ▼         ▼
    DANGER            WARNING      SAFE
    (🔴)              (🟡)        (🟢)
        │                 │         │
    ┌───┴──────────┬──────┴─────┬──┴────┐
    │              │            │       │
    ▼              ▼            ▼       ▼
 Red Card     Yellow Card   Green Card  Text
 + Alert      + Alert       + Message   Message
 Msg          Msg

Example: RICE
──────────────
Current: 38°C, 50% humidity

                Temp Check
                    │
         ┌──────────┴──────────┐
         │                     │
     38°C > 37°C?          50% < 70%?
         │ YES               │ YES
         ▼                   ▼
    ADD: 🔥 High          ADD: 💨 Low
    Temperature Alert     Humidity Alert
    Severity: DANGER      Severity: WARNING
         │                   │
         └────────┬──────────┘
                  │
           Risk Level: DANGER
           (Takes highest severity)
                  │
                  ▼
        Display Red Alert Card:
        "🍚 RICE - 🔴 DANGER
         🔥 High temp warning
         💨 Low humidity warning"
```

## API Request Flow

```
USER INTERACTION
         │
         ▼
┌─────────────────────────────────┐
│  handleFetchWeather(city)      │
│  handleUseCurrentLocation()    │
└─────────────┬───────────────────┘
              │
         ┌────┴────┐
         │         │
         ▼         ▼
    GET /data/2.5/weather        GET /data/2.5/forecast
    ?q={city}                    ?q={city}
    ?units=metric                ?units=metric
    ?appid=KEY                   ?appid=KEY
         │                            │
         │         OpenWeatherMap     │
         └────────────┬───────────────┘
                      │
              ┌───────┴────────┐
              │                │
              ▼                ▼
        Response: {       Response: {
          name,             list: [{
          main: {             dt,
            temp,             main: {temp, humidity},
            feels_like,       weather: {...},
            humidity,         rain: {...}
            pressure          },...]
          },                }
          weather: {...},
          wind: {...},
          clouds: {...}
        }
              │                │
              └────────┬───────┘
                       │
                       ▼
            State Update + Parse
            setWeatherData()
            setForecastData()
            setLastUpdated()
                       │
                       ▼
              calculateWeatherAlerts()
                       │
                       ▼
              setCurrentAlerts()
                       │
                       ▼
                   UI Render
```

## Risk Level Visualization

```
SAFE (🟢)                    WARNING (🟡)              DANGER (🔴)
─────────────────────────────────────────────────────────────────

Optimal Conditions        Suboptimal but OK       Critical Conditions
Green background          Yellow background        Red background
Opacity: 1.0             Opacity: 1.0             Opacity: 1.0 + Pulse
No animation             No animation            Pulsing animation

Examples:                Examples:                Examples:
- 25°C for Rice          - 36°C for Rice         - 38°C for Rice
- 60% humidity           - 65% humidity          - 40% humidity
- Normal rainfall        - Low rainfall          - Heavy rainfall
- All crops good         - Monitor closely       - Take action now!
```

## User Interaction Workflow

```
LANDING ON PAGE
       │
       ▼
┌──────────────────┐
│ Load Gujarat     │
│ weather          │
└────────┬─────────┘
         │
         ▼
   ┌─────────────┐
   │ 3 Options:  │
   └─────┬───────┘
         │
    ┌────┼─────────────────┐
    │    │                 │
    ▼    ▼                 ▼
  SEARCH GEOLOCATION   SELECT CROPS
   CITY  "Use Location"   (Optional)
    │    │                 │
    │    └────────┬────────┘
    │             │
    └─────────────┼─────────────┐
                  │             │
            ┌─────▼──────┐  ┌──▼────────┐
            │  API Call  │  │ Local     │
            │            │  │ Calc      │
            └─────┬──────┘  └──┬───────┘
                  │            │
                  └────────┬───┘
                           │
                           ▼
                   Display Results
                   - Weather Card
                   - Alert Cards
                   - Forecast Data
                           │
                           ▼
                   ┌─────────────────┐
                   │ Enable Notif?   │
                   │ (Optional)      │
                   └────────┬────────┘
                            │
                        ┌───┴────┐
                        │        │
                        ▼        ▼
                      YES       NO
                        │        │
                    Grant     Skip
                    Perm.      │
                        │        │
                        └───┬────┘
                            │
                            ▼
                    ✅ System Ready
```

## Error Handling Flow

```
API REQUEST FAILS
       │
       ▼
┌────────────────┐
│ Catch Error    │
│ Block          │
└────────┬───────┘
         │
    ┌────┴────────────────────────┐
    │                             │
    ▼                             ▼
ERROR TYPE?              EXTRACT MESSAGE
    │                        │
    ├─ Network Error    Error instanceof
    │                   Error.message
    ├─ Invalid City     │
    │                   ▼
    ├─ Permission       FORMAT MESSAGE
    │ Denied        "Failed to fetch..."
    │                   │
    ├─ Geolocation      ▼
    │ Not Available  DISPLAY TO USER
    │                   │
    └─ Unknown      ┌───┴─────┐
                    │         │
                    ▼         ▼
               Toast Msg   setError()
               (Feedback)  (State)
                    │         │
                    │         ▼
                    │    UI shows error
                    │    in red box
                    │         │
                    │    User can retry
                    │
                    ▼
              Auto-dismiss
              after 4 seconds
```

---

## Summary

This architecture provides:
- ✅ **Clear data flow** from user input to UI
- ✅ **Modular components** with single responsibilities
- ✅ **Efficient state management** with React hooks
- ✅ **Robust error handling** at every step
- ✅ **Scalable design** for future enhancements
- ✅ **User-friendly workflows** with feedback

The system is optimized for **performance**, **reliability**, and **user experience**.
